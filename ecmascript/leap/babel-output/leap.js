// class Leap {
//   let isLeapYear = function(year) {
//   }
// }
//
// export default Leap;

'use strict';

var isLeapYear = function isLeapYear(year) {
  if (year % 4 !== 0) {
    return 'is not very common.';
  }
};